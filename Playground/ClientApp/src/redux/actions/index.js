import loginStateUpdate from './LoginStateUpdate';
import loginShowModalUpdate from './LoginShowModalUpdate';

// Combine all actions into a single list
const actions = {
  loginStateUpdate,
  loginShowModalUpdate
};

export default actions;